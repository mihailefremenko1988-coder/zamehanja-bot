services:
  - type: web
    name: zamechaniya-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: HUGGINGFACE_API_KEY
        sync: false
